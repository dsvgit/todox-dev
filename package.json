{
  "name": "todox",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "http-server -c-1 -p 3000",
    "postinstall": "cp .hooks/pre-commit .git/hooks",
    "format": "prettier . --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "@preact/signals-core": "^1.8.0",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3"
  }
}
